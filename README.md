# pushbutton
This project demonstrates an Arduino Servo Motor control using a Push Button. When the button is pressed, the servo motor rotates to 90 degrees (ON) and stays active for 10 seconds. After the delay, the servo motor automatically returns to 0 degrees (OFF). 
